{
  "name": "pecs_m4-257",
  "version": "1.0.0",
  "description": "",
  "browserslist": "last 10 years, not dead",

  "scripts": {
    "parcel:dev": "parcel src_P1/index.html --dist-dir .parcel-temp --open",
    "parcel:build": "parcel build src_P1/index.html --dist-dir dist --public-url ./",
    "clean:dev": "rimraf .parcel-cache .parcel-temp",
    "clean:prod": "rimraf dist .parcel-cache",
    "start": "npm-run-all clean:dev parcel:dev",
    "build": "npm-run-all clean:prod parcel:build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "parcel": "^2.16.4",
    "posthtml-include": "^2.0.1",
    "rimraf": "^6.1.3"
  },
  "dependencies": {
    "smooth-scrollbar": "^8.8.4"
  }
}
